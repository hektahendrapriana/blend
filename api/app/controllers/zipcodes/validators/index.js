const { validateCreateZipcode } = require('./validateCreateZipcode')
const { validateDeleteZipcode } = require('./validateDeleteZipcode')
const { validateGetZipcode } = require('./validateGetZipcode')
const { validateUpdateZipcode } = require('./validateUpdateZipcode')

module.exports = {
  validateCreateZipcode,
  validateDeleteZipcode,
  validateGetZipcode,
  validateUpdateZipcode
}

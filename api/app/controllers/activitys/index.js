const { createActivity } = require('./createActivity')
const { deleteActivity } = require('./deleteActivity')
const { getAllActivitys } = require('./getAllActivitys')
const { getActivity } = require('./getActivity')
const { getActivitys } = require('./getActivitys')

module.exports = {
  createActivity,
  deleteActivity,
  getAllActivitys,
  getActivity,
  getActivitys
}
